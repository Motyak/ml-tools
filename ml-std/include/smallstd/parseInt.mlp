var parseInt (OUT str):{
    var acc 0
    until(():{len(str) == 0 || str[#1] !in '0 .. '9}, ():{
        acc := acc * 10 + Int(str[#1] - Byte("0"))
        str := tern(len(str) == 1, "", str[#2..-1])
    })
    acc
}
